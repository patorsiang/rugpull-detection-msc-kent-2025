# WARNING: This exposes Redis without auth. Lock down at the network/firewall level.
protected-mode no
bind 0.0.0.0

# Persistence
appendonly yes
appendfsync everysec
save 900 1
save 300 10
save 60 10000

# Data dir (mapped by Docker)
dir /data
